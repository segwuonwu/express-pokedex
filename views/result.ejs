<div class="container">
    <h1>Favorite Pokemons</h1>

    <% pokemon.forEach((poke) => { %>
        <div class="well">
            <a href="/pokemon/<%= poke.name %>">
                <%= poke.name %>
            </a>
        </div>
        <% }); %>
</div>